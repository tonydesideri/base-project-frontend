import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    background: {
      default: '#FAFAFA' // Cor de fundo global
    },
    primary: {
      main: '#FF5000'
    }
  }
});
